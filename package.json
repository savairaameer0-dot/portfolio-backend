{
  "name": "portfolio-backend",
  "version": "1.0.0",
  "description": "A small Express server I built to learn backend basics — serves data as a real API instead of hardcoding it into the frontend.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "cors": "^2.8.6",
    "express": "^5.2.1"
  }
}
